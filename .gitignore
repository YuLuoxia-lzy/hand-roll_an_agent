# 密钥 —— 绝对不能提交
.env
.env.local
*.key

# 记忆层默认落盘位置(相对 CWD) —— 向量库和对话记录不该进版本库
data/
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# 学习笔记与离线测试 —— 只留在本地, 不往仓库推
tests/
docs/

# 这个示例要用 tests/_harness.py 里的假 embedder / 假 LLM, 跟着 tests 一起留在本地
examples/memory_demo.py

# 工具
.vscode/
.idea/
.ipynb_checkpoints/
