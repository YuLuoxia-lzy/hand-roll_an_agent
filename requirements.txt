# ==================== 核心依赖 ====================
# 这两个是必需的, 没有就跑不起来
openai>=1.0.0
pydantic>=2.0.0

# ==================== 可选依赖 ====================
# 下面这些都**不是**必需的, 按需取消注释安装。

# --- 搜索工具的后端 ---
# 不装也能用框架, 只是 SearchTool 没有任何可用后端, 调用它会返回一段配置提示
# tavily-python
# google-search-results

# --- 自动加载 .env 文件 ---
# 装了的话, import Agent_0_to_1 时会自动读取同目录的 .env;
# 没装就静默跳过, 你可以继续用系统环境变量
# python-dotenv
